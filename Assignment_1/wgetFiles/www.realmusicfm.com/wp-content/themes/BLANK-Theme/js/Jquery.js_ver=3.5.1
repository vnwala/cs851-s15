$(document).ready(function(){
	
///MODAL WINDOW////////////////
//select all the a tag with name equal to modal

 //$("img[rel]").overlay();$('a[rel]').overlay();

	
///$("#chat_info").overlay({
 
  //   custom top position
 //   top: 200,
 
 //   some mask tweaks suitable for facebox-looking dialoge
 
 //   load it immediately after the construction
  //  load: true
 
   // });
   
	//add pipe after li a
	/*$('#top_nav ul li a').after('<span>|</span>');
	
	if($('#top_nav ul li').hasClass('last')){
		
	$('#top_nav ul li a').after('');
	}*/
	$("#top_nav ul li a").after("<span class='divider'>|</span>");
	//$('#top_nav ul li a').last().after('<span> </span>');
	$("#member_info span:last").remove(".divider");
	$("#menu-main_menu span:last").remove(".divider");
	
});