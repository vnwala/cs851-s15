(function ($) {var self = $.animate.products = {		init: function () {		self.resize();		self.searchbar();		// self.watched_anime();	},		resize: function () {		$(window).resize(self.searchbar);	},		searchbar: function () {		var search = $('#search');				search.find('> div').width(search.width() - 260);		search.find('input[type=text]').width(parseInt(search.find('> div form').width() - 130 - 10));	},		// watched_anime: function () {	// 	var watched_anime = $('#watched_anime');	// 	var li            = watched_anime.find('li');			// 	watched_anime.find('div.more a').click(function () {	// 		li.removeClass('hide');	// 		$(this).parent().hide();				// 		return false;	// 	});	// }	}$(function () { self.init(); });})(jQuery);