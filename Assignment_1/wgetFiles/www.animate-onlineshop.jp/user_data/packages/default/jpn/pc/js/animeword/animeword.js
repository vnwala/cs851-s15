(function ($) {var self = $.animate.animeword = {    init: function () {        self.resize();        self.searchbar();        self.accordion_all();        self.accordion_item();    },    resize: function () {        $(window).resize(self.searchbar);    },    searchbar: function () {        var search;        if ( $('#search').size() > 0 ) {            var search    = $('#search');            var min_input = 260;            var min_btn   = 130;        }        else {            var search = $('#search_animeword');            var min_input = 230;            var min_btn   = 162;        }        var search = $('#search').size() > 0 ? $('#search') : $('#search_animeword');            search.find('> div').width(search.width() - min_input);            search.find('input[type=text]').width(parseInt(search.find('> div form').width() - min_btn - 10));    },    accordion_all: function () {        $( ".ac_open" ).css( "display", "none" );        $( "#related_products a.close_all" ).click( function() {            var tar = $( "#related_products > ul" );            var img = $( "#related_products .close_item img" );            var src = img.attr( 'src' );            var new_src;            if ( $('.ac_close').is(':visible') ) {                $( ".ac_close" ).css( "display", "none" );                $( ".ac_open" ).css( "display", "block" );                tar.slideUp("slow");                new_src = src.replace("minus", "plus");            }            else {                $( ".ac_close" ).css( "display", "block" );                $( ".ac_open" ).css( "display", "none" );                tar.slideDown("slow");                new_src = src.replace( "plus", "minus" );            }            $( img ).attr( "src", new_src );        });    },    accordion_item: function () {        $( "a.close_item" ).click( function() {            var tar = $( this ).parent().next( "ul" );            tar.toggleClass( "close" ).slideToggle( "slow" );            self.icon( $( this ).children( "img") );        });    },    icon: function ( img ) {        var src = img.attr('src');        if ( src.indexOf( "minus" ) >= 0 ) {            new_src = src.replace("minus", "plus");        }        else {            new_src = src.replace("plus", "minus");        }        $( img ).attr( "src", new_src );    }}$(function () { self.init(); });})(jQuery);